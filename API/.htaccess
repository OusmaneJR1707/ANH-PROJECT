<IfModule mod_rewrite.c>
    RewriteEngine On 

    RewriteRule ^$ public/index.php [L]
    # ?! è un negative lookhead, guarda nei caratteri successivi per vedere se non c'è public, nel caso procede ad eseguire l'istruzione
    RewriteRule ^((?!public/).*)$ public/$1 [L] 
</IfModule>